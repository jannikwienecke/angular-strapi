'use strict';

/**
 * karriere-benefit controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::karriere-benefit.karriere-benefit');
